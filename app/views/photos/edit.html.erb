<h1>UploadPhoto</h1>
<%= form_for @photo, url: album_photo_path(@album, @photo), :method => :put do |f| %>
<p>
	<%= f.label :image %><br>
	<%= f.file_field :image %><br>
	<%= f.submit 'Upload Photo' %>
   		or
   	<%= link_to 'Cancel', [@album, @photo] %>
 </p>

<% end %>